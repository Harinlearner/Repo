#include <bits/stdc++.h>
using namespace std;

int main() 
{
    int n;
    cin>>n;
    vector<bool>is_Prime(n,true);
    is_Prime[0]=is_Prime[1]=false;
    for(int i=2;i*i<=n;i++)
    {
      if(is_Prime[i])
      {
        for(int j=i*i;j<=n;j+=i)
        {
          if(j%i==0)
          {
            is_Prime[j]=false;
          }
        }
      }
    }
    for(int i=0;i<=n;i++)
    {
      if(is_Prime[i])
      cout<<i<<" ";
    }
    
    
}
